/**
 * Generic logger
 * @module logger
 */
const common = require('./common');
let externalLogger;

/**
 * Log trace entry
 * @param {string} msg - the input raw command 
 * @param {string} err - the input raw command 
 */
module.exports.trace = (msg, err) => {
    if (common.logger.trace) {
        console.log('[TRACE] ' + msg);
    }

    if(externalLogger){
        externalLogger.trace(msg);
    }
}

/**
 * Log debug entry
 * @param {string} msg - the input raw command 
 * @param {string} err - the input raw command 
 */
module.exports.debug = (msg, err) => {
    if (common.logger.debug) {
        console.log('[DEBUG] ' + msg);
    }

    if(externalLogger){
        externalLogger.debug(msg);
    }
}

/**
 * Log info entry
 * @param {string} msg - the input raw command 
 * @param {string} err - the input raw command 
 */
module.exports.info = (msg, err) => {
    if (common.logger.info) {
        console.log('[INFO] ' + msg);
    }
    
    if(externalLogger){
        externalLogger.info(msg);
    }
}

/**
 * Log error entry
 * @param {string} msg - the input raw command 
 * @param {string} err - the input raw command 
 */
module.exports.error = (msg, err) => {
    console.log('[ERROR] ' + msg);
    if(externalLogger){
        externalLogger.error(msg);
    }
}

/**
 * Setting External Logger
 * @param {string} logger - the input raw command 
 */
module.exports.setExternalLogger = (logger) =>{
    externalLogger = logger;
};


//////////////////
// WEBPACK FOOTER
// ./node_modules/besst-usb-sdk/src/usbClient/logger.js
// module id = 108
// module chunks = 73